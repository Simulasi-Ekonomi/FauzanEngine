#!/usr/bin/env sh
APP_HOME=$(cd "$(dirname "$0")" && pwd)
JAVA_OPTS=""
GRADLE_OPTS="${GRADLE_OPTS:-"-Dfile.encoding=UTF-8 -Dkotlin.daemon.jvm.options=-Xmx2g"}"
exec "$APP_HOME/gradle/wrapper/gradle-wrapper" "$@" 2>/dev/null || \
  java -jar "$APP_HOME/gradle/wrapper/gradle-wrapper.jar" "$@"
